<%- include ../partials/navbar %>
<%- include ../partials/notifications %>

<div class="container">
  <h3 class="bill-title">My active bills</h3>

  <div class="list-group">
    <% bills.forEach((bill) => { %>
      <% if (bill.active) {%>
        <a href="/bills/list/<%= bill._id %>" class="list-group-item list-group-item-action extra-margin">
          <div class="flex-between">
            <span class="bill-name" value="<%= bill.items %>"><%= bill.name %></span>
            <span class="debt"><span id="user-total">10</span> €</span>
          </div>
        </a>
      <% } %>
    <% }) %>
  </div>
</div>

<a href="/" class="centered go-back"><i class="fas fa-angle-double-left"></i></a>
